Why would I use the filter function?
    You use the filter function to go through a lot of information quickly and filter out the specific information that you are looking for. 
Why would I use the map function?
    Map is changing the shape of our array 
Why would I use the reduce function?
    reduce is to create subsets within the array, showing all of the objects with the same, singular variable within the array. 